import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6DAEDB',
    },
    secondary: {
      main: '#A3D6AD',
    },
    error: {
      main: '#FFA07A',
    },
    background: {
      default: '#FFFFFF',
    },
    text: {
      primary: '#4A4A4A',
    },
  },
});

export default theme;
